--- items.c.orig	2026-08-12 12:00:00.000000000 +0000
+++ items.c	2026-08-12 12:05:00.000000000 +0000
@@ -1375,10 +1375,8 @@
 static uint64_t lru_total_bumps_dropped(void) {
     uint64_t total = 0;
     lru_bump_buf *b;
     pthread_mutex_lock(&bump_buf_lock);
     for (b = bump_buf_head; b != NULL; b=b->next) {
-        pthread_mutex_lock(&b->mutex);
         total += b->dropped;
-        pthread_mutex_unlock(&b->mutex);
     }
     pthread_mutex_unlock(&bump_buf_lock);
     return total;
 }

--- extstore.c.orig	2026-08-12 12:00:00.000000000 +0000
+++ extstore.c	2026-08-12 12:05:00.000000000 +0000
@@ -681,7 +681,5 @@
         } else {
             p->obj_count = 0; // caller has bad accounting?
         }
-        STAT_L(e);
         e->stats.bytes_evicted += bytes;
-        STAT_UL(e);
         pthread_mutex_unlock(&p->mutex);
     }
